ls -al ~/.ssh
# Lists the files in your .ssh directory, if they exist
$ ls -al ~/.ssh
$ls -al ~/.ssh
id_dsa.pub
ssh-keygen -t rsa -C "altmane01@gmail.com"
$ ssh-keygen -t rsa -C "altmane01@gmail.com"
$ssh-keygen -t rsa -C "altmane01@gmail.com"
ssh-keygen -t rsa -C "altmane01@gmail.com"
git --version
git init
readme
